import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Workspace Management — Cloud Care Pharmacy",
};

export default function WorkspaceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
